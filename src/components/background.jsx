const Background = ({ children }) => {
  return (
    <div className="container grid min-h-[100svh] place-items-center content-center">
      {children}
    </div>
  );
};

export default Background;
